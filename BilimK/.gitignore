# Секреты — никогда не должны попадать в Git
.env
.streamlit/secrets.toml

# База данных (создаётся локально, не должна лежать в репозитории)
data/*.db

# Служебные файлы Python
__pycache__/
*.pyc
.venv/
venv/
